version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20             # Specifies the Node.js runtime version
  pre_build:
    commands:
      - docker pull 977527528431.dkr.ecr.ap-south-1.amazonaws.com/backend:latest  # Forces npm to install the packages
  build:
    commands:
artifacts:
  files: 
    - '**/*'              # Includes all files and subdirectories from the root directory.
    - 'appspec.yml'          # Includes the appspec.yml file
    - 'scripts/**' 
