version: 0.2
phases:             
  pre_build:
    commands:
      - docker build -t nodeimage .
      - docker run -d -p 8000:5000 --name nodecontainer --restart unless-stopped nodeimage
      - docker tag nodeimage:latest 977527528431.dkr.ecr.ap-south-1.amazonaws.com/backend:latest
      - docker images
      - docker push docker push 977527528431.dkr.ecr.ap-south-1.amazonaws.com/backend:latest
  build:
    commands:
artifacts:
  files: 
    - '**/*'              # Includes all files and subdirectories from the root directory.
    - 'appspec.yml'          # Includes the appspec.yml file
    - 'scripts/**' 
